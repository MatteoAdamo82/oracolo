## a simple app who predicts Lotto numbers

Use `docker-compose up`

## requirements
- docker desktop installed and running in your machine

## tests
docker-compose -f docker-compose-test.yaml up --build
(pytest without docker)