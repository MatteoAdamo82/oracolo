[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=app --cov-report=html --cov-report=term-missing
markers =
    cli: tests for the CLI interface
    integration: integration tests
    unit: unit tests